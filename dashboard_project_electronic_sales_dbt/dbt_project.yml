name: 'dashboard_project_electronic_sales' 

profile: 'dashboard_project_electronic_sales'  

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dashboard_project_electronic_sales:
    weekly_aggregates:
      +materialized: table
      +enabled: true
      +schema: final_dataset
